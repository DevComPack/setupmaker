<?xml version="1.0" encoding="UTF-8" standalone="no" ?>

<installation version="1.0"
			xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
			xmlns:xi="http://www.w3.org/2001/XInclude">

    <!-- 
        The info section.
        The meaning of the tags should be natural ...
    -->
    <xi:include href="res/xml/InfoSpec.xml" />

    <!-- 
        The gui preferences indication.
        Sets the installer window to 800x600. It will not be able to change the size.
    -->
    <guiprefs width="800" height="600" resizable="no">
    	<laf name="looks">
        	<os family="windows" />
        	<param name="variant" value="windows" />
    	</laf>
    	<laf name="metouia">
        	<os family="unix" />
    	</laf>
    	
		<modifier key="langDisplayType" value="native"/>
		<modifier key="allXGap" value="0"/>
		<modifier key="allYGap" value="0"/>
		<modifier key="labelGap" value="2"/>
		<modifier key="useButtonIcons" value="yes"/>
		<modifier key="useHeadingPanel" value="yes"/>
		<modifier key="useHeadingForSummary" value="yes"/>
    	<modifier key="headingLineCount" value="1"/>
		<modifier key="headingFontSize" value="1.5"/>
		<modifier key="headingBackgroundColor" value="0x00ffffff"/>
		<modifier key="headingPanelCounter" value="text"/>
    </guiprefs>
    
    <!-- 
        The locale section.
        Asks here to include the English and French langpacks.
    -->
    <locale>
        <langpack iso3="fra"/>
        <langpack iso3="eng"/>
    </locale>
    
    <!-- 
    	Variables section
    -->
	<xi:include href="res/xml/Variables.xml" />
    
    <!-- 
    	The Packaging section.
    	We indicate the packaging element to span the installer over multiple volumes.
    -->
    <xi:include href="res/xml/PackagingSpec.xml" />

    <!-- 
        The panels section.
        We indicate here which panels we want to use. The order will be respected.
    -->
    <xi:include href="res/xml/PanelsSpec.xml" />

    <!-- The listeners section for CustomActions -->
    <xi:include href="res/xml/Listeners.xml" />

    <!--
        The resources section.
        The ids must be these ones if you want to use the LicencePanel and/or the InfoPanel.
    -->
	<xi:include href="res/xml/Resources.xml" />
    
    <!-- 
        The jar section.
        We specify here our libraries.
    -->
    <jar src="lib/dcp/dcp-executable.jar" stage="both"/>
    
    <!-- 
    	The Natives section.
    	We specify here our dll files.
    	Registry + Shortcuts support
    -->
    <native type="izpack" name="ShellLink.dll">
	    <os family="windows"/>
	</native>
    <native type="izpack" name="ShellLink_x64.dll">
	    <os family="windows"/>
	</native>
    <native type="3rdparty" name="COIOSHelper.dll">
	    <os family="windows"/>
	</native>
    <native type="3rdparty" name="COIOSHelper_x64.dll">
	    <os family="windows"/>
	</native>

    <!-- 
        The packs section.
        We specify here our packs.
    -->
    <packs>
   		<pack name="core" required="yes">
            <description>Mandatory files for setup. Must be installed!</description>
            <file src="res/Readme.txt" targetdir="$INSTALL_PATH" override="true" />
            <file src="res/bat/clean.bat" targetdir="$INSTALL_PATH/Uninstaller" override="true" />
            <file src="res/utils/Elevate.exe" targetdir="$INSTALL_PATH/Uninstaller" override="true" />
        </pack>
        
	    <xi:include href="res/xml/PacksPanelSpec.xml" />
    </packs>

</installation>